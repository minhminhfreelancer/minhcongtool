# Enable legacy OpenSSL provider for Cloudflare Pages
node-options=--openssl-legacy-provider

# Use strict SSL
strict-ssl=true

# Configure npm to use Node.js 18.x compatible versions
engine-strict=true

# Optimize installation
save-exact=true
fund=false

# Cache configuration
cache-min=3600
cache-max=86400